# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local external-docs sync artifacts (CI clones content into these)
/external-docs/
/i18n/
# NOTE: /docs/ is committed as a local fallback so the site builds offline;
# CI replaces it with content from verificahub-docs at build time.
